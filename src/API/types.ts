export interface Dates {
  // name: string;
  // legal_form: string;
  // headcount: string[];
  // company_size: string[];
  // inactive: boolean;
  // is_branch: boolean;
  // area: string;
}

export interface Status {
  // staus: string;
}
